[package]
name = "slt-runtime-abort"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
superlighttui = { path = "../../..", features = ["async"] }
tokio = { version = "1", features = ["rt", "time"] }

[profile.dev]
panic = "abort"
